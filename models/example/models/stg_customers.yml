version: 2

models:
  - name: stg_customers
    description: "Staging model pro zákazníky"
    columns:
      - name: customer_number
        description: "Customer primary key"
        tests:
          - not_null
          - unique
